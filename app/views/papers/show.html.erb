<% if current_user.has_role? :admin %>

<%- model_class = Paper -%>

<div class="container col-xs-12 col-sm-10 col-md-8 affix">
  <div class="btn-toolbar pull-right" role="toolbar" arial-label="toolbar">
    <!-- 管理功能 -->
    <div class="btn-group" role="group" arial-label="manage">
      <% if current_user.id == @paper.user_id  %>
        <%= link_to t('.new', default:  t("helpers.links.new")), 
          new_paper_path,
          class:  'btn btn-default', 
          tabindex: 21, 
          "data-toggle": "tooltip", 
          "data-placement": "bottom", 
          title: t('.new', default: [:"helpers.titles.new"], model: model_class.model_name.human.titleize) %>
        <%= link_to t('.edit', default: t("helpers.links.edit")), 
          edit_paper_path(@paper),
          class: 'btn btn-default', 
          tabindex: 22, 
          "data-toggle": "tooltip", 
          "data-placement": "bottom", 
          title: t('.edit', default: [:"helpers.titles.edit"], model: model_class.model_name.human.titleize) %>
        <%= link_to t('.destroy', default: t("helpers.links.destroy")),
          paper_path(@paper),
          method: 'delete',
          data: { confirm: t('.confirm', default: t("helpers.links.confirm", default: 'Are you sure?')) },
          class: 'btn btn-default', 
          tabindex: 23, 
          "data-toggle": "tooltip", 
          "data-placement": "bottom", 
          title: t('.delete', default: [:"helpers.titles.delete"], model: model_class.model_name.human.titleize) %>
      <% end %>
      <%= link_to t('.back', default: t("helpers.links.back")),
        papers_path,
        class: 'btn btn-default', 
        tabindex: 24, 
        "data-toggle": "tooltip", 
        "data-placement": "bottom", 
        title: t('.back', default: [:"helpers.titles.back"], model: model_class.model_name.human.titleize)  %>
    </div>
   <!-- 附加功能 -->
    <div class="btn-group" role="btn-group" arial-label="operation">
      <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        <span class="caret"></span>
      </button>
      <ul class="dropdown-menu">
        <li>
        </li>
        <li role="separator" class="divider"></li>
      </ul>
    </div>
    <!-- 导航按钮 -->
    <div class="btn-group" role="btn-group" arial-label="navigation">
      <% if @previous_paper %>
        <%= link_to t('.previous', default: t("helpers.links.previous")), 
          paper_path(@previous_paper), 
          class: 'btn btn-default', 
          tabindex: 61, 
          "data-toggle": "tooltip", "data-placement": "bottom", 
          title: t('.previous', default: t("helpers.titles.previous"), model: t("activerecord.models.paper")) %>
      <% end %>
      <% if @next_paper %>
        <%= link_to t('.next', default: t("helpers.links.next")), 
          paper_path(@next_paper), 
          class: 'btn btn-default', 
          tabindex: 62, 
          "data-toggle": "tooltip", 
          "data-placement": "bottom", 
          title: t('.next', default: t("helpers.titles.next"), model: t("activerecord.models.paper")) %>
      <% end %>
      <%= link_to t('.retreat', default: t("helpers.links.retreat")), 
        :back, 
        class: 'btn btn-default', 
        tabindex: 81, 
        "data-toggle": "tooltip", 
        "data-placement": "bottom", 
        title: t('.retreat', default: [:"helpers.titles.retreat"]) %>
    </div>
  </div>
</div>

<div class="page-header">
  <h3><%=t '.title', default: model_class.model_name.human.titleize %></h3>
</div>

<dl class="dl-horizontal">
  <dt><strong><%= model_class.human_attribute_name(:user_id) %>:</strong></dt>
  <dd><%= @paper.user_id %></dd>
  <dt><strong><%= model_class.human_attribute_name(:title) %>:</strong></dt>
  <dd><%= @paper.title %></dd>
  <dt><strong><%= model_class.human_attribute_name(:duration) %>:</strong></dt>
  <dd><%= @paper.duration %></dd>
  <dt><strong><%= model_class.human_attribute_name(:deleted_at) %>:</strong></dt>
  <dd><%= @paper.deleted_at %></dd>
</dl>

<% else %>

<%- model_class = Paper -%>

<div class="container col-xs-12 col-sm-10 col-md-8 affix">
  <div class="btn-toolbar pull-right" role="toolbar" arial-label="toolbar">
    <!-- 管理功能 -->
    <div class="btn-group" role="group" arial-label="manage">
      <% if current_user.id == @paper.user_id  %>
        <%= link_to t('.new', default:  t("helpers.links.new")), 
          new_paper_path,
          class:  'btn btn-default', 
          tabindex: 21, 
          "data-toggle": "tooltip", 
          "data-placement": "bottom", 
          title: t('.new', default: [:"helpers.titles.new"], model: model_class.model_name.human.titleize) %>
        <%= link_to t('.edit', default: t("helpers.links.edit")), 
          edit_paper_path(@paper),
          class: 'btn btn-default', 
          tabindex: 22, 
          "data-toggle": "tooltip", 
          "data-placement": "bottom", 
          title: t('.edit', default: [:"helpers.titles.edit"], model: model_class.model_name.human.titleize) %>
        <%= link_to t('.destroy', default: t("helpers.links.destroy")),
          paper_path(@paper),
          method: 'delete',
          data: { confirm: t('.confirm', default: t("helpers.links.confirm", default: 'Are you sure?')) },
          class: 'btn btn-default', 
          tabindex: 23, 
          "data-toggle": "tooltip", 
          "data-placement": "bottom", 
          title: t('.delete', default: [:"helpers.titles.delete"], model: model_class.model_name.human.titleize) %>
      <% end %>
      <%= link_to t('.back', default: t("helpers.links.back")),
        papers_path,
        class: 'btn btn-default', 
        tabindex: 24, 
        "data-toggle": "tooltip", 
        "data-placement": "bottom", 
        title: t('.back', default: [:"helpers.titles.back"], model: model_class.model_name.human.titleize)  %>
    </div>
   <!-- 附加功能 -->
    <div class="btn-group" role="btn-group" arial-label="operation">
      <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        <span class="caret"></span>
      </button>
      <ul class="dropdown-menu">
        <li>
          <%= link_to "查看答卷记录", papertests_url %>
        </li>
        <%#<li role="separator" class="divider"></li>%>
      </ul>
    </div>
    <!-- 导航按钮 -->
    <div class="btn-group" role="btn-group" arial-label="navigation">
      <% if @previous_paper %>
        <%= link_to t('.previous', default: t("helpers.links.previous")), 
          paper_path(@previous_paper), 
          class: 'btn btn-default', 
          tabindex: 61, 
          "data-toggle": "tooltip", "data-placement": "bottom", 
          title: t('.previous', default: t("helpers.titles.previous"), model: t("activerecord.models.paper")) %>
      <% end %>
      <% if @next_paper %>
        <%= link_to t('.next', default: t("helpers.links.next")), 
          paper_path(@next_paper), 
          class: 'btn btn-default', 
          tabindex: 62, 
          "data-toggle": "tooltip", 
          "data-placement": "bottom", 
          title: t('.next', default: t("helpers.titles.next"), model: t("activerecord.models.paper")) %>
      <% end %>
      <%= link_to t('.retreat', default: t("helpers.links.retreat")), 
        :back, 
        class: 'btn btn-default', 
        tabindex: 81, 
        "data-toggle": "tooltip", 
        "data-placement": "bottom", 
        title: t('.retreat', default: [:"helpers.titles.retreat"]) %>
    </div>
  </div>
</div>

<div class="page-header">
  <h3><%=t '.title', default: model_class.model_name.human.titleize %> >> <%= @paper.title %></h3>
</div>

<div class="well">
  <div class="row">
  <div class="col-xs-4">
    <p>
      <strong><%= model_class.human_attribute_name(:user_id) %>: </strong><%= @paper.user.name %>
    </p>
  </div>
  <div class="col-xs-4">
    <p>
      <strong><%= model_class.human_attribute_name(:created_at) %>: </strong><%= @paper.created_at.strftime("%F %T") %>
    </p>
  </div>
  <div class="col-xs-4">
    <p>
    </p>
  </div>
  </div>
  <div class="row">
  <div class="col-xs-4">
    <p>
      <strong>总分: </strong><%= number_with_precision(@paper.paperitems.collect{|pi|pi.score}.inject(:+), precision: 2) %>
    </p>
  </div>
  <div class="col-xs-4">
    <p>
      <strong>总题量: </strong><%= @paper.paperitems.count %>
    </p>
  </div>
  <div class="col-xs-4">
    <p>
      <strong><%= model_class.human_attribute_name(:duration) %>: </strong><%= @paper.duration %>分钟
    </p>
  </div>
  </div>
</div>
<% if current_user.id == @paper.user_id %>
<%= link_to "编辑/查看试卷", paperitems_url, class: 'btn btn-success' %>
<% elsif Master.find(current_user.id) %>
<%= link_to "查看试卷", paperitems_url, class: 'btn btn-success' %>
<% else %>
<%= link_to "开始测试", paperitems_url, class: 'btn btn-success' %>
<% end %>

<% if @papertests.any? %>

<button class="btn btn-default pull-right" type="button" data-toggle="collapse" data-target="#collapsepapertests" aria-expanded="false" aria-controls="collapseAddToCardbox">
  查看答卷记录
</button>
<div class="collapse" id="collapsepapertests">
<table class="table table-striped">
  <thead>
    <tr>
      <th>标识号</th>
      <th>开始时间</th>
      <th>得分</th>
      <th>答卷情况</th>
    </tr>
  </thead>
  <tbody>
    <% @papertests.each do |papertest| %>
      <tr>
        <td><%= link_to papertest.id, papertest_path(papertest) %></td>
        <td><%= papertest.created_at.strftime("%F %T") %></td>
        <% if papertest.evaluations.count == 0 %>
          <td><%= number_with_precision(0, precision: 2) %></td>
        <% elsif papertest.evaluations.map{|e| e.score ? 1 : 0}.inject(:+) == papertest.evaluations.count %>
          <td><%= number_with_precision(papertest.evaluations.map{|e| e.score }.inject(:+), precision: 2) %></td>
        <% else %>
          <td>试卷还没改好</td>
        <% end %>
          <td><%= link_to "查看答卷情况", papertest_url(papertest.id), class: "btn btn-xs btn-default" %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>
<% end %>

<% end %>
