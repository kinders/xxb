<%= form_for @papertest, html: { class: "form-horizontal papertest" } do |f| %>

    <% if @papertest.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@papertest.errors.count, "error") %> 使得这个 papertest 无法保存：</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @papertest.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :user_id, class: 'control-label col-sm-2' %>
    <div class="controls col-sm-10">
      <%= f.text_field :user_id, class: 'form-control' %>
    </div>
    <%= f.error_span(:user_id) %>
  </div>
  <div class="form-group">
    <%= f.label :paper_id, class: 'control-label col-sm-2' %>
    <div class="controls col-sm-10">
      <%= f.text_field :paper_id, class: 'form-control' %>
    </div>
    <%= f.error_span(:paper_id) %>
  </div>
  <div class="form-group">
    <%= f.label :end_at, class: 'control-label col-sm-2' %>
    <div class="controls col-sm-10">
      <%= f.text_field :end_at, class: 'form-control' %>
    </div>
    <%= f.error_span(:end_at) %>
  </div>
  <div class="form-group">
    <%= f.label :deleted_at, class: 'control-label col-sm-2' %>
    <div class="controls col-sm-10">
      <%= f.text_field :deleted_at, class: 'form-control' %>
    </div>
    <%= f.error_span(:deleted_at) %>
  </div>


  <%= f.submit nil, class: 'btn btn-primary col-sm-offset-2' %>
  <%= link_to t('.cancel', default: t("helpers.links.cancel")),
            papertests_path, class: 'btn btn-default' %>

<% end %>
